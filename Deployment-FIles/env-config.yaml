apiVersion: v1
kind: ConfigMap
metadata:
  name: env-vars
data:
  SECRET_KEY: "Payment-gateway-secret-key"
  DEBUG: "True"
  DATABASE_ENGINE: "django.db.backends.postgresql"
  DATABASE_NAME: "kinomart_new"
  DATABASE_USER: "postgres"
  DATABASE_PASSWORD: "1234"
  DATABASE_HOST: "postgres-service"
  EMAIL_HOST_USER: "email-adrress"
  EMAIL_HOST_PASSWORD: "password"
